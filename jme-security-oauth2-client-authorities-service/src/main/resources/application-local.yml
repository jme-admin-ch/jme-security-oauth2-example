server:
  port: 8092

target:
  resource: "http://localhost:8072/jme-security-oauth2-resource-authorities-service"

spring:
  security:
    oauth2:
      client:
        provider:
          default-provider:
            issuer-uri: "http://localhost:8081/jme-security-oauth2-auth-scs"
